import { ProjectCountPipe } from './project-count.pipe';

describe('ProjectCountPipe', () => {
  it('create an instance', () => {
    const pipe = new ProjectCountPipe();
    expect(pipe).toBeTruthy();
  });
});
