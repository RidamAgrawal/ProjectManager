import { EndDateValidDirective } from './end-date-valid.directive';

describe('EndDateValidDirective', () => {
  it('should create an instance', () => {
    const directive = new EndDateValidDirective();
    expect(directive).toBeTruthy();
  });
});
